
#include <stdio.h>
#define NUMBER 80
int main(void)
{

for (i = 9; i >= 0; i--)
	{
		printf("%3d - %3d:", i * 10, i * 10 + 9);
		//		for (j =0; i < bunpu[i]; j++)
		//	putchar('*');
		putchar('\n');
	}

 return (0);

}
